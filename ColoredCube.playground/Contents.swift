// The famous cube with each of the 6 faces shaded
// with a different color.
// An instance of Timer is added to support animation. The "render:"
// method of SPOpenGLView is called at 1/60-th sec.
// Requirements: XCode 8.3.2 macOS 10.12.x or later.

import PlaygroundSupport

// Create instance of ViewController and tell playground
// to show it in live view
let vc = SPViewController()
PlaygroundPage.current.liveView = vc
