// Demonstrates how to use the gl_VertexID variable to set the
// color attribute of a vertex. The fragment shader will interpolate the color.
// Requirements: XCode 8.3.2 macOS 10.12.x or later.

import PlaygroundSupport

let vc = SPViewController()
PlaygroundPage.current.liveView = vc
